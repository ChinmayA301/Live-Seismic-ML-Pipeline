# Direct dependencies. CI installs from requirements.lock (fully pinned,
# including transitives) — regenerate it after editing this file:
#   uv pip compile requirements.txt --universal --python-version 3.11 \
#     --output-file requirements.lock
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
duckdb>=1.0
matplotlib>=3.7
requests>=2.28
pyyaml>=6.0
joblib>=1.3
great-expectations>=1.0
mlflow>=2.9,<3
evidently>=0.7
tabulate>=0.9
